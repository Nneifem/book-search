{
  "name": "book-search",
  "version": "1.0.0",
  "description": "search for any book you want!",
  "main": "server/server.js",
  "engines": {
    "node": "18.x",
    "npm": "7.x"
  },
  "scripts": {
    "start": "node server/server.js",
    "develop": "concurrently \"cd server && npm run watch\" \"cd client && npm run dev\"",
    "install": "cd servre && npm i && cd ../client && npm i",
    "build": "cd client && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
